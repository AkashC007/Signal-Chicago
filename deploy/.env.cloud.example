# The cloud provider will store these values as managed secrets. Do not commit
# real values or copy the local .env file into a deployment archive.
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/signal_chicago
CTA_TRAIN_API_KEY=
COLLECTION_INTERVAL_SECONDS=120
STATION_CONFIG_PATH=config/stations.json
MAX_RESULTS_PER_STATION=5

